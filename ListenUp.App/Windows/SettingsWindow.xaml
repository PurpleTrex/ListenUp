<Window x:Class="ListenUp.App.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings" Height="500" Width="600" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Application Settings" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Gutenberg API Settings -->
                <TextBlock Text="Project Gutenberg API" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <Grid Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Base URL:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="GutenbergUrl" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="API Key Name:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="GutenbergKeyName" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="API Key Value:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                    <PasswordBox Grid.Row="2" Grid.Column="1" x:Name="GutenbergKeyValue" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Host Header:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBox Grid.Row="3" Grid.Column="1" x:Name="GutenbergHost" Margin="0,0,0,8"/>
                </Grid>

                <!-- Cache Settings -->
                <TextBlock Text="Cache Settings" FontSize="16" FontWeight="Bold" Margin="0,10,0,10"/>
                <Grid Margin="0,0,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Cache Duration (hours):" VerticalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="CacheDuration" Text="24" Margin="0,0,0,8"/>

                    <Button Grid.Row="1" Grid.Column="1" Content="Clear Cache" Click="OnClearCache" Padding="8,4" HorizontalAlignment="Left"/>
                </Grid>

                <!-- About -->
                <TextBlock Text="About" FontSize="16" FontWeight="Bold" Margin="0,10,0,10"/>
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,10">
                    ListenUp is a public domain book reader and audiobook player.<LineBreak/>
                    Version 1.0.0<LineBreak/>
                    Data sources: Project Gutenberg, LibriVox, Open Library
                </TextBlock>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Click="OnSave" Width="100" Padding="8,4" Margin="0,0,10,0" IsDefault="True"/>
            <Button Content="Cancel" Click="OnCancel" Width="100" Padding="8,4" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
